{
    "name": "robtic-monorepo",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "type": "module",
    "scripts": {
        "build": "bun run build:packages && bun run build:apps",
        "build:apps": "bun run --filter './apps/*' build",
        "build:packages": "bun run --filter './packages/*' build",
        "dev": "bun run --filter './apps/*' dev",
        "test": "bun run --filter './**' test"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "chalk": "^5.6.2",
        "dotenv": "^17.2.3"
    }
}